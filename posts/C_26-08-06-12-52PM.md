## memset函数

```c
#include <stdio.h>
#include <string.h>

int main() {
    char arr[] = "hello world";
    memset(arr, 'x', 5);
    printf("%s\n", arr);  // xxxxx world
    memset(arr + 6, 'x', 5);
    printf("%s\n", arr);  // xxxxx xxxxx
    return 0;
}
```
